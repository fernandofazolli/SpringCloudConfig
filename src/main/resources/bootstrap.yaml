spring:
  application:
    name: SpringCloudConfigApplication
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fernandofazolli/cloud_config
  application:
    name: config

server:
  port: 8001